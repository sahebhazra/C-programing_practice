#include<stdio.h>
int main()
{
    int width ,length, area ;
    
    printf("width is: ");
    scanf("%d", &width);

    printf("length is: ");
    scanf("%d", &length);

    printf("Area of rectangle is %d\n", area= width*length);
    return 0;

}